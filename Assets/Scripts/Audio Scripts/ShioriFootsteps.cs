using UnityEngine;
using System.Collections;
using System.Collections.Generic;

// public class ShioriFootsteps : MonoBehaviour
//{
    // public AudioClip sfx_pc_stephard_fd1;
    // private PlayerMovement playerMovement;

    //void Start()
    //{
       // playerMovement = GetComponent<PlayerMovement>();
       // StartCoroutine(PlayShioriFootstep());
   // }

  //IEnumerator PlayShioriFootstep()
   // {
        // while (true)
       // {
            // if (playerMovement.playerMovement.magnitude > 0.1f)
           // {
                // AudioManager.instance.PlaySFX(sfx_pc_stephard_fd1);
                //yield return new WaitForSeconds(0.35f);
            // }
            // else
            //{
                //yield return new WaitForSeconds(0.1f);
           // }
        //}
   // }
//}
